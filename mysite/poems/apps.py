from django.apps import AppConfig


class PoemsConfig(AppConfig):
    """This is a simple setup for the poem class."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'poems'
